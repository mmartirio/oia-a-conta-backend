package com.oiaaconta.order.enums;

// Espelha catalog-service.enums.TipoDesconto — usado só pra interpretar a
// resposta do CatalogClient (validação de cupom / promoções aplicáveis).
public enum TipoDesconto {
    PERCENTUAL, FIXO
}
